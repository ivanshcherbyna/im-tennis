//not finished code
    function filterMode(obj, mode){
    if($('.products-filter-mode').hasClass('active')){
        $(obj).removeClass('active');
    }
    $(obj).addClass('active');

        console.log(mode);
    }
